# Leslie Memory Vault rules

Markdown files in this vault are the only durable fact source. SQLite is a disposable search index.

Before writing, search and run prewrite reconciliation. Never store secrets or raw chat transcripts. After editing, claim the exact changed files for the current actor/session and run closeout. Search results are candidates; reread the Markdown source before relying on a fact.

